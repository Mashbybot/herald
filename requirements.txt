# Core Discord bot dependencies
discord.py>=2.3.0,<3.0.0
python-dotenv>=1.0.0

# Database dependencies
# SQLite is built into Python, but we need PostgreSQL for production
asyncpg>=0.28.0

# Optional but recommended for production
aiohttp>=3.8.0        # HTTP client (used by discord.py)
typing-extensions>=4.0.0  # Type hints compatibility

# Production monitoring and logging (optional)
# Uncomment these if you want advanced monitoring:
# sentry-sdk>=1.32.0    # Error tracking and monitoring
# psutil>=5.9.0         # System resource monitoring
